{
  "name": "vs-vote",
  "scripts": {
    "start": "next",
    "build": "next build",
    "serve": "yarn build && next start",
    "export": "yarn build && next export",
    "deploy": "yarn build && next export && firebase deploy"
  },
  "license": "ISC",
  "dependencies": {
    "next": "^7.0.2",
    "react": "^16.6.0",
    "react-dom": "^16.6.0"
  },
  "devDependencies": {
    "antd": "^3.10.4",
    "axios": "^0.18.0",
    "babel-plugin-import": "^1.11.0",
    "babel-plugin-inline-import": "^3.0.0",
    "styled-components": "^4.0.3"
  }
}
